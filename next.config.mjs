/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["unsplash.com","firebasestorage.googleapis.com",'scontent-yyz1-1.cdninstagram.com',"static.wikia.nocookie.net",'www.wordstream.com'] // Add the hostname of your image source
      },
};

export default nextConfig;
